import React from 'react';
import { Collapse, CardBody, Card, Form, FormGroup, Label, Input } from 'reactstrap';

export default function AddSkill({ isOpen }) {
    return (
        <Collapse isOpen={isOpen}>
            <Card>
                <CardBody>
                </CardBody>
            </Card>
        </Collapse>
    );
}

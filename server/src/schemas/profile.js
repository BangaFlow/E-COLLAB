/*
import Joi from 'joi'

const phone = Joi.string().regex('/d{3} d{8}/').label('phone');
const bio = Joi.string().max(600).label('Bio');


export const updateMyProfile = Joi.object().keys({
    phone, skills, bio, github_username, profile_id
});
*/
